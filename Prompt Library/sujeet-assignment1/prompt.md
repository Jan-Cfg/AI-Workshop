Act as a Senior Software Engineer. I need to design a C# .NET utility class that wraps the GdPicture SDK for image processing.

Objectives:

Implement functionality to increase image DPI.

Implement functionality for merging, splitting, and downscaling/reducing pixel depth of TIFF images.

Constraints & Requirements:

Follow SOLID principles. The code must be testable, so use Interfaces (Dependency Injection).

Use a clean, modular design (e.g., separating file IO from image manipulation logic).

No hardcoding (use configuration/options patterns).

